TDLAS data reading and fitting 

Change variables _shotCount_ (for all data types) and _folderName_ (for picking newest updated file in folder) 

Install required packages to your local environment: 

	pip install  -r requirements.txt
